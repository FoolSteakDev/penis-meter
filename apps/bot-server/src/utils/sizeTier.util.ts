interface SizeTier {
  min: number;
  labels: string[];
}

// Відсортовано за спаданням `min` - перший тир, де value >= min, і є відповіддю.
const SIZE_TIERS: SizeTier[] = [
  {
    min: 40,
    labels: ['Аномалія природи 🌌', 'Порушення законів фізики', 'Восьме диво світу', 'Треба викликати НАСА'],
  },
  {
    min: 30,
    labels: ['Легенда 👑', 'Міфічна істота', 'Той, про кого складають пісні', 'Ходяча легенда району'],
  },
  {
    min: 23,
    labels: ['Величезний 🍆', 'Монстр', 'Достаток природи', 'Небезпечно для оточуючих'],
  },
  {
    min: 19,
    labels: ['Великий', 'Серйозний калібр', 'Вражаючий результат', 'Є чим пишатись'],
  },
  {
    min: 16,
    labels: ['Вище середнього', 'Непогано, непогано', 'Трохи більше за норму', 'Впевнений результат'],
  },
  {
    min: 13,
    labels: ['Середній', 'Норма', 'Класика жанру', 'Як у більшості'],
  },
  {
    min: 10,
    labels: ['Нижче середнього', 'Скромний результат', 'Є куди рости', 'Не найгірший варіант'],
  },
  {
    min: 7,
    labels: ['Малий', 'Компактний формат', 'Економ-варіант', 'Малий, але свій'],
  },
  {
    min: 0,
    labels: ['Мікропеніс', 'Ледь помітно', 'Наноспис', 'Мінімалізм в дії'],
  },
  {
    min: -10,
    labels: ['Мікропізда (реальна діра)', 'Вже не пеніс, а діра', 'Перейшов на іншу сторону', 'Точка неповернення'],
  },
  {
    min: Number.NEGATIVE_INFINITY,
    labels: ['Чорна діра 🕳️', 'Портал в інший вимір', 'Сингулярність', 'Там немає навіть світла'],
  },
];

function pickRandom<T>(items: T[]): T {
  return items[Math.floor(Math.random() * items.length)];
}

export function getSizeTierLabel(value: number): string {
  const tier = SIZE_TIERS.find((t) => value >= t.min) ?? SIZE_TIERS[SIZE_TIERS.length - 1];
  return pickRandom(tier.labels);
}
